name: CI

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  actions: read
  contents: read
  packages: write # ✅ Ajoute les permissions nécessaires pour publier sur GHCR

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Cache Node.js modules
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # Installer les dépendances
      - run: npm ci --legacy-peer-deps

      # Installer Playwright si utilisé
      - run: npx playwright install --with-deps

      # Initialiser Nx pour la CI
      - uses: nrwl/nx-set-shas@v4

      # Lint, Test et Build (Nx affecté uniquement)
      - run: npx nx affected -t lint test build

  docker:
    needs: main # Le build Nx doit réussir avant
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker images
        run: |
          docker-compose build
          docker tag backend ghcr.io/${{ github.repository_owner }}/backend:latest
          docker tag frontend ghcr.io/${{ github.repository_owner }}/frontend:latest
          docker push ghcr.io/${{ github.repository_owner }}/backend:latest
          docker push ghcr.io/${{ github.repository_owner }}/frontend:latest
